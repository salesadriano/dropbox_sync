# Memoria de Projeto dos Agents

> Arquivo versionavel para registrar decisoes de demandas concretas do projeto em andamento.

## Regras de persistencia

- Todo agent deve ler este arquivo antes de atuar, em conjunto com `MEMORIA-COMPARTILHADA.md`.
- Este arquivo deve manter decisoes do projeto e da demanda atual: escopo, requisitos, arquitetura, implementacao, validacao, riscos, aceite e backlog de trabalho.
- Decisoes transversais sobre agents, skills, workflow, templates e governanca do pacote devem ser registradas em `MEMORIA-COMPARTILHADA.md` e tambem refletidas nesta memoria de projeto, com referencia cruzada.
- Quando houver solicitacao explicita para registrar em ambos os escopos, a mesma decisao deve ser persistida nas duas memorias com referencia cruzada.
- Detalhes extensos, cronologia de mudancas e evidencias completas devem ficar em `historico/`.

> **Memoria reinicializada em 2026-08-17 por decisao do solicitante.** O conteudo anterior descrevia o projeto de origem do pacote (`ai_team`) e nao este workspace. Nao foi migrado para `historico/` porque permanece integro na origem: `/home/sales/ai_team/.github/agents/memoria/MEMORIA-PROJETO.md`. Isso encerra `DIV-09` e `RSK-16`.

## Contexto do projeto atual

| Campo | Valor |
|---|---|
| Projeto | Aplicacao CLI em shell script para integracao com a Dropbox API v2 |
| Workspace | `/home/sales/dropbox_api` |
| Foco atual | **Etapa 2 em andamento.** Dominio aprovado com ressalva (`lib/hash`, `lib/errors`, `lib/path`); **`lib/json` e `lib/output` entregues**. Requisitos e System Design na **v0.6**. **Nenhuma decisao bloqueia a Etapa 2** — `DP-05` fechou o desenho de `lib/config`, e `RNF-23` fixou a regra de paginacao que `lib/http` deve seguir. **`DP-20` (titular do copyright) segue como unica pendencia com custo crescente** |
| Repositorio | ✅ `github.com:salesadriano/dropbox_sync`, branch `master` publicada. **Gitflow ativo**: `develop` criado a partir de `master`, trabalho em `feature/*`, Conventional Commits a partir deste ponto. Desvios consumados, **nao reescrever historico**: commit inicial sem convencao semantica, e `LICENSE` publicado com titular do copyright em **espaco reservado** |
| Estado | Ativo e versionado |
| Stack detectada | **Shell script + `cURL`**, confirmada com a primeira entrega de codigo. **Agora tambem DECIDIDA, nao apenas observada:** `DP-07` fixou **Linux com `bash` 4+** (piso tecnico **4.4**) e `DP-08` fixou **`cURL` como unica dependencia**, com interpretador JSON proprio. Skill de referencia: `.claude/skills/protocolo-tdd/` |
| Referencia conceitual | `/home/sales/Dropbox-Uploader` (GPLv3) — modelo de estudo, **sem derivacao de codigo** |
| Responsavel de consolidacao | Tech Lead |

## Decisoes ativas de projeto

| ID | Decisao | Impacto no projeto | Dono | Status |
|---|---|---|---|---|
| PRJ-DEC-01 | Reimplementacao independente. O `Dropbox-Uploader` e referencia conceitual apenas; nao copiar nem adaptar codigo GPLv3. | Libera a escolha de licenca e evita herdar os defeitos confirmados no modelo (segredo em `argv`, endpoint de busca retirado, ausencia de `mktemp`). Resolve `DP-01`. Licenca especifica permanece aberta em `DP-20`. | Solicitante | Ativa |
| PRJ-DEC-02 | A aplicacao atende uso interativo **e** automatizado (cron/CI). | Exige deteccao de TTY, saida estruturada parseavel, codigos de saida deterministicos e ausencia de prompt bloqueante sem TTY. Promoveu `RF-15` e `RF-28` a P0. Resolve `DP-03`. | Solicitante | Ativa |
| PRJ-DEC-03 | Alvo: conta pessoal com acesso a Dropbox inteira (nao restrito a pasta do aplicativo). | Ampliou o raio de exposicao em caso de vazamento de credencial. Colocou `RF-06` (Business/Team) fora de escopo, criou `RNF-20` (menor privilegio e confinamento de raiz) e `RSK-19`. Resolve `DP-04`. | Solicitante | Ativa |
| PRJ-DEC-04 | Escopo do MVP: Bloco 0 (base endurecida) + `F-01` fluxo `stdin`/`stdout` + `F-02` incremental por `content_hash` + `F-04` contrato de automacao + `F-05` relatorio de execucao. | Nenhuma das quatro exige estado local persistente, o que mantem a arquitetura simples e **nao** aciona o handoff do DBA. `F-03` (retomada) fica fora do MVP. Resolve `DP-02`, ultima decisao bloqueante. | Solicitante | Ativa |
| PRJ-DEC-05 | Os 6 agents de persona receberam a ferramenta `ToolSearch`. | Sem ela nenhum agent alcanca o Context7 MCP, cujas ferramentas sao de carregamento diferido, tornando o item 28 do protocolo inaplicavel na pratica. Os subagents utilitarios (`documentation-writer`, `commit-writer`) permanecem restritos. **Decisao transversal: refletir em `MEMORIA-COMPARTILHADA.md`.** | Solicitante | Ativa |
| PRJ-DEC-06 | Memoria de projeto reinicializada, descartando o conteudo herdado do pacote de origem. | Encerra `DIV-09` e `RSK-16`. Decisoes deste projeto passam a acumular aqui, nao apenas em `docs/`. | Solicitante | Ativa |
| PRJ-DEC-07 | **Invariante arquitetural: o MVP nao possui estado local persistente.** A unica escrita persistente e o arquivo de configuracao com a credencial — sem cursor, catalogo, cache de resumos, indice ou arquivo de trava. | Decorre de `PRJ-DEC-04`, que deixou fora do escopo as quatro funcionalidades que exigiriam persistencia (`F-03`, `F-06`, `F-12`, `F-14`). Mantem `DP-09` fechada e o handoff do DBA nao acionado. Reintroduzir persistencia e **mudanca de escopo** e exige nova decisao do solicitante. Risco de vigilancia: `RSK-23`. | Business Analyst | Ativa |
| PRJ-DEC-09 | **Camada de dominio implementada com TDD**: `lib/hash.sh`, `lib/errors.sh`, `lib/path.sh` e suite propria em `tests/`. Vetor oficial de RF-34 **confirmado experimentalmente** contra o arquivo real da Dropbox, e nao adiado. | Apos o ciclo 1 de QA: **113 casos aprovados** com o vetor oficial habilitado, 111 mais 2 pulados sem rede. `shellcheck` 0.10.0 com **exit 0** e 7 supressoes justificadas — `RNF-13` atendido. Sem `bats` no ambiente, adotado harness proprio com saida TAP 13, trocavel por `bats` sem alterar o contrato de relatorio. Registro em `docs/registros/`. | Senior Developer | Ativa, aguardando reavaliacao do QA |
| PRJ-DEC-12 | **Caminho e devolvido por variavel (`DBX_PATH_RESULTADO`), nao so por saida padrao.** A substituicao de comando do shell remove quebras de linha finais, e quebra de linha e byte valido em nome de arquivo. | Defeito `D1` do ciclo 1 de QA: o confinamento devolvia `arq` com status 0 para uma solicitacao de `arq\n`, fazendo ler ou sobrescrever o arquivo errado reportando sucesso. Toda funcao publica de `lib/path` grava o valor exato na variavel; a impressao continua como conveniencia. | Senior Developer | Ativa |
| PRJ-DEC-13 | **`lib/hash` le cada bloco para um arquivo de buffer reaproveitado, sem nenhum cano no caminho de calculo.** | Defeitos `D4` e `D3`: sem cano, o status do leitor e observavel sem `pipefail` (antes, leitor com falha gerava `content_hash` bem formado de conteudo truncado com status 0) e o tamanho de cada bloco fica disponivel, permitindo expor os bytes lidos exigidos por `RF-31`. Custo assumido: uma escrita e uma leitura por bloco. A area temporaria deve migrar para `lib/tmp` na Etapa 2. | Senior Developer | Ativa |
| PRJ-DEC-14 | **O agregado da suite trafega por canal proprio (`DBX_HARNESS_RESUMO`), nunca por `eval` da saida dos testes; filtro sem correspondencia reprova.** | Defeito `D5`: o executor avaliava linha vinda do stdout dos proprios testes, permitindo execucao de comando arbitrario e forja dos totais, e aprovava com zero arquivos executados. Sem isso, nenhum numero da suite serve como evidencia. | Senior Developer | Ativa |
| PRJ-DEC-15 | **`dbx_errors_politica_retentativa` recebe a idempotencia da operacao.** Falha de transporte em operacao idempotente devolve `recuo_exponencial`; em nao idempotente, `indeterminado`. `408` passa a ser retentavel. | `PRJ-DEC-02` exige cron/CI, e antes um RST de TCP encerrava o lote com saida 9 e zero retentativa **inclusive** em `download`, `list_folder` e `get_metadata`. `indeterminado` nomeia o estado real ("nao da para saber se a escrita foi aplicada") em vez de instruir "nao tente". A mesma ambiguidade em `5xx` ficou **em aberto**. | Solicitante | Ativa |
| PRJ-DEC-16 | **A taxonomia de erro casa TAGS e remove qualificadores de uniao** (`path`, `path_lookup`, `path_write`, `from_lookup`, `from_write`, `to`, `lookup_failed`) antes do casamento. | Defeito `D5` do QA: a familia de erros de rota colapsava em `desconhecido` (saida 1), esvaziando `RF-29`. A remocao **exige a barra**, entao a fronteira de componente nao regride — ha caso de nao regressao dedicado e mutacao que o comprova. | Solicitante | Ativa |
| PRJ-DEC-17 | **Raiz `/` exige autorizacao explicita (`raiz_total=sim`); sem ela, falha fechado.** | Operar sobre a conta inteira era padrao silencioso: nada no uso revelava que a protecao de `RNF-20` estava inativa. A opcao autoriza so a raiz `/` e nao desliga a normalizacao. | Solicitante | Ativa |
| PRJ-DEC-25 | **`lib/json` indexa por NO, sem caminho concatenado.** A unica composicao e `<id do pai><sep><segmento>`, injetiva porque o lado esquerdo e sempre digito. | Defeitos `E2-01`/`E2-02`: chave com o separador por escape unicode designava outro campo — a decodificacao vinha depois da verificacao. Corrigir a ordem fecharia o sintoma e manteria a classe, que e a mesma de `DIV-04`. **Medido: eliminar a concatenacao melhorou ~10%** (800 entradas, 882 ms para 790 ms). | Solicitante/QA | Ativa |
| PRJ-DEC-26 | **Invariante de projeto: nenhum canal de dado externo transita por `$( )`.** Verificada por auditoria estatica em `lib/*.sh`, com lista de excecoes justificada (alfabeto fechado e comprimento limitado). | Terceira ocorrencia da classe: `D1` (Etapa 1), `C2-01` (ciclo 2) e `E2-04`. Regra verificavel substitui disciplina. **Correcao de afirmacao (`E3-04`):** o padrao original exigia `$(` logo apos `=` e deixava passar `+=`; havia ocorrencia viva. Ampliado. | Coordenador | Ativa |
| PRJ-DEC-28 | **Classe conhecida: "instrumento de observacao interfere na propriedade observada".** Quarta ocorrencia registrada. | `assert_status` capturando em subshell · conversao de terminador nulo antes de redigir (duas vezes) · sondas do proprio QA · `grep` do coordenador cegado por byte NUL no documento. **Correcao de afirmacao minha:** eu disse que o `assert_status` "escondia o `E2-09`"; o QA mediu por reversao (218/0/2, zero reprovacoes) e mostrou que o risco era **prospectivo**, nao retroativo — a suite fora escrita contornando a limitacao. Agora pinado por `E3-07`. | QA | Ativa |
| PRJ-DEC-29 | **`lib/json` expoe enumeracao inequivoca** (`dbx_json_nome_da_filha` por variavel e `dbx_json_chaves_nul`); `dbx_json_chaves` fica como conveniencia com limitacao declarada. | `E3-02`: nome com quebra de linha produzia 3 linhas para 2 filhas — consulta correta, enumeracao ambigua. Mesmo problema que `DIV-16b` resolveu em `lib/output`. `E3-03`: contador passou a medir chaves distintas, e nao ocorrencias sintaticas. | QA | Ativa |
| PRJ-DEC-27 | **`lib/output` redige por valor ao alimentar o modelo**, e nao sobre o fluxo renderizado; diagnostico vai para a saida de ERRO. | A protecao deixa de depender de alguem a jusante lembrar de redigir, e o isolamento extra de linha no modo nulo (`E2-12`) tornou-se desnecessario. `RF-28` (P0) exige diagnostico em `stderr`. | QA | Ativa |
| PRJ-DEC-22 | **`lib/json` e analisador proprio com varredura em passada unica; sem `jq`, sem `sed` por campo.** Falha fechada e classificada por `DBX_JSON_MOTIVO`. | Alternativas **medidas antes de escolher**: caractere a caractere 17/22/48/98 ms contra marcacao+divisao 4/4/8/16 ms nos mesmos tamanhos. Valor devolvido por variavel, nao so impresso — `$( )` remove quebras finais (licao `D1`/`C2-01`). | Senior Developer | Ativa, pendente de QA |
| PRJ-DEC-23 | **Teto de entrada de `lib/json` = 256 KiB, dimensionado por custo medido**, nao por folga abstrata. | Custo com expoente ~1,45 (a afirmacao de linearidade estava **errada** e foi corrigida): 800 entradas / 42.900 chars = 882 ms. Um teto de 4 MiB extrapolaria para ~86 s — seria repetir o defeito `R-04`. **Requisito derivado para `lib/http`:** paginar listagem com `limit` explicito, senao pasta grande falha por recusa de analise. | Senior Developer | Ativa |
| PRJ-DEC-24 | **`lib/output` separa terminador (linha/nulo) de apresentacao (humana/estruturada)** — dimensoes ortogonais. `RNF-22` implementado por isolamento de registro, com quebra de linha explicita antes do terminador nulo em cabecalho sensivel. | Sem essa quebra, a redacao — que opera por linha inclusive no modo nulo — atravessaria o byte nulo e levaria o identificador de correlacao de `RF-30`. | Senior Developer | Ativa |
| PRJ-DEC-21 | **`lib/output` nao pode concatenar campos de diagnostico na mesma linha de um cabecalho sensivel.** | Em cabecalho sensivel a redacao consome ate o fim da LINHA. Em dump HTTP o efeito e inocuo; em registro de uma linha so, o que vier depois se perde — inclusive o identificador de requisicao que `RF-30` existe para preservar. Requisito de entrada para a Etapa 2. | Senior Developer | Ativa |
| PRJ-DEC-19 | **Metodo, nao sintoma: `dbx_errors_redigir` e varredura em passada unica; nenhum `$( )` no componente de caminho, raiz inclusa.** | Duas rodadas seguidas trocaram um defeito por outro equivalente no mesmo lugar (redacao: vazamento -> quadratica -> cubica; caminho: `$()` publico -> `$()` na raiz -> evasao de confinamento). Enquanto o desenho for "iterar padroes sobre a cadeia inteira", o custo depende do conteudo e nenhum teto o contem. Varredor indexado por caractere foi **medido e descartado**: `${cadeia:indice:1}` e quadratico mesmo em `LC_ALL=C`. | Solicitante | Ativa |
| PRJ-DEC-20 | **A politica de retentativa consulta a classificacao**, em vez de decidir por codigo HTTP. | `409 too_many_write_operations` era classe `limite_taxa` com politica `nenhuma` — contencao de lock que o servico manda repetir, abortando o lote. Politica nova `retomar` para `incorrect_offset`, distinta de `reiniciar`. | Solicitante | Ativa |
| PRJ-DEC-18 | **Confinamento local aceito como escopo** (`DIV-D`), alem do que `RNF-20` previa. | O Business Analyst emendara `RNF-20` para nomear os dois espacos, remoto e local. | Solicitante | Ativa |
| PRJ-DEC-10 | **Tabela de codigos de saida de RF-29 fixada em 0 a 15**, com a faixa reservada do shell (126, 127, 128+n) deixada livre. | Apenas `0`, `2`, `3` e `4` decorriam de criterios de aceite ja aprovados; `5` a `15` eram proposta da entrega da camada de dominio. **Aceite do Tech Lead concedido em 2026-08-18 (`TL-01`)**, apos verificar a condicao imposta pelo QA — coerencia entre classe de erro e politica de retentativa, corrigida em `PRJ-DEC-20` — e a existencia de teste que reprova qualquer mudanca de valor. A faixa e agora **contrato publico permanente**: mudar o significado de um codigo exige nova versao principal, por RF-35. Encerra `DIV-17`. | Senior Developer (proposta), Solicitante (aceite do contrato), Tech Lead (gate) | **Ativa — aceite concedido** |
| PRJ-DEC-11 | **`lib/errors` nao depende de `lib/json`**, ao contrario do que a tabela de componentes do System Design registra. Recebe codigo HTTP e `error_summary` ja extraidos. | Preserva a regra de dependencia do proprio System Design ("Dominio: sem dependencia externa"), desacopla a taxonomia de `DP-08` e respeita a exclusao de `jq`. Divergencia `DIV-A`: corrigir o System Design. | Senior Developer | Ativa |
| PRJ-DEC-08 | Algoritmo do `content_hash` confirmado na fonte oficial: blocos de 4 MiB, SHA-256 por bloco, concatenacao **binaria** dos resumos, SHA-256 final, saida hexadecimal de 64 caracteres. Ha vetor de teste oficial. | Desbloqueia `lib/hash`, `RF-33` e `RF-34`, e com eles a funcionalidade `F-02`. Rebaixa `RSK-21`. Atencao de implementacao: concatenar as representacoes hexadecimais em vez dos bytes brutos produz resultado errado que so aparece na comparacao com a API. | Business Analyst | Ativa |
| PRJ-DEC-25 | **`lib/json` indexa por NO, sem caminho concatenado.** A unica composicao e `<id do pai><sep><segmento>`, injetiva porque o lado esquerdo e sempre digito. | Defeitos `E2-01`/`E2-02`: chave com o separador por escape unicode designava outro campo — a decodificacao vinha depois da verificacao. Corrigir a ordem fecharia o sintoma e manteria a classe, que e a mesma de `DIV-04`. **Medido: eliminar a concatenacao melhorou ~10%** (800 entradas, 882 ms para 790 ms). | Solicitante/QA | Ativa |
| PRJ-DEC-26 | **Invariante de projeto: nenhum canal de dado externo transita por `$( )`.** Verificada por auditoria estatica em `lib/*.sh`, com lista de excecoes justificada (alfabeto fechado e comprimento limitado). | Terceira ocorrencia da classe: `D1` (Etapa 1), `C2-01` (ciclo 2) e `E2-04`. Regra verificavel substitui disciplina. **Correcao de afirmacao (`E3-04`):** o padrao original exigia `$(` logo apos `=` e deixava passar `+=`; havia ocorrencia viva. Ampliado. | Coordenador | Ativa |
| PRJ-DEC-28 | **Classe conhecida: "instrumento de observacao interfere na propriedade observada".** Quarta ocorrencia registrada. | `assert_status` capturando em subshell · conversao de terminador nulo antes de redigir (duas vezes) · sondas do proprio QA · `grep` do coordenador cegado por byte NUL no documento. **Correcao de afirmacao minha:** eu disse que o `assert_status` "escondia o `E2-09`"; o QA mediu por reversao (218/0/2, zero reprovacoes) e mostrou que o risco era **prospectivo**, nao retroativo — a suite fora escrita contornando a limitacao. Agora pinado por `E3-07`. | QA | Ativa |
| PRJ-DEC-29 | **`lib/json` expoe enumeracao inequivoca** (`dbx_json_nome_da_filha` por variavel e `dbx_json_chaves_nul`); `dbx_json_chaves` fica como conveniencia com limitacao declarada. | `E3-02`: nome com quebra de linha produzia 3 linhas para 2 filhas — consulta correta, enumeracao ambigua. Mesmo problema que `DIV-16b` resolveu em `lib/output`. `E3-03`: contador passou a medir chaves distintas, e nao ocorrencias sintaticas. | QA | Ativa |
| PRJ-DEC-27 | **`lib/output` redige por valor ao alimentar o modelo**, e nao sobre o fluxo renderizado; diagnostico vai para a saida de ERRO. | A protecao deixa de depender de alguem a jusante lembrar de redigir, e o isolamento extra de linha no modo nulo (`E2-12`) tornou-se desnecessario. `RF-28` (P0) exige diagnostico em `stderr`. | QA | Ativa |
| PRJ-DEC-22 | **Etapa 1 — camada de dominio: APROVADO COM RESSALVAS.** Aceite tecnico concedido; a camada esta apta a ser consumida pela Etapa 2. **Fechamento formal da entrega suspenso** — nao por qualidade, e sim por `DP-20` (titular do copyright) e `DP-19` (repositorio git). | Verificacao independente do Tech Lead em 2026-08-18: suite 155/0/2 (157/0/0 com rede), `shellcheck` exit 0, ausencia de git e `LICENSE` com espaco reservado confirmados. Ratificados nesta decisao: `PRJ-DEC-18` (confinamento local), `C2-08` (transito de `stdin` por `$TMPDIR`, sem ressalva, dispensa de nota operacional respeitada) e `RSK-24` (TOCTOU). Sem escalonamento por reprovacao: 2 contra o limite de 3. Artefatos: `docs/registros/2026-08-18_revisao-consolidada-etapa-1-camada-dominio.md` e `docs/registros/2026-08-18_aprovacao-final-etapa-1-camada-dominio.md`. | Tech Lead | Ativa |
| PRJ-DEC-23 | **Gate de processo (`TL-09`): premissa de escopo nao vira decisao do solicitante.** Toda premissa de plataforma, ambiente, dependencia externa ou escopo que restrinja a implementacao deve ser levantada como pedido de decisao ao solicitante **antes** de ser implementada. Observacao de ambiente (`stack detectada`) **nao** e decisao de escopo. | Deriva de `RSK-25`: em `DIV-15` uma premissa de plataforma foi registrada como decisao do solicitante sem que ele a tivesse tomado. O padrao **ja reincidiu** e foi barrado a tempo em `RSK-24`, ao recusar `/proc/self/fd` — o gate existe para que a licao nao dependa de memoria individual. **Candidata a decisao transversal do pacote; `MEMORIA-COMPARTILHADA.md` NAO foi alterada, por determinacao do solicitante.** | Tech Lead | Ativa |
| PRJ-DEC-24 | **Desvios de protocolo da Etapa 1 homologados.** Cypress inaplicavel (item 13) e template de validacao QA de frontend inaplicavel (itens 17 e 19): sem interface web ou grafica e sem Design System. Ausencia de artefatos `.md` do QA e de acionamento do `prompt-logger` pelo QA (itens 2 e 4): homologada **com compensacao obrigatoria**, por ter decorrido de instrucao do proprio Tech Lead. | O conteudo do gate independente e justamente o que mais precisa sobreviver — tres ciclos, cinco bloqueantes no primeiro, cinco no segundo com duas regressoes, e uma troca de metodo determinada pelo solicitante. Compensacao produzida: `docs/registros/2026-08-18_validacao-qa-camada-dominio-registro-retroativo.md`, rotulado como reconstrucao retroativa e nao como parecer original. **Gate instituido: a partir da Etapa 2, nenhum ciclo de QA fecha sem artefato proprio e sem log de prompt.** | Tech Lead | Ativa |

## Backlog de projeto

| Item | Estado |
|---|---|
| `content_hash` (`DIV-14`) | ✅ **Confirmado** — ver `PRJ-DEC-08`. `lib/hash` desbloqueado |
| `X-Dropbox-Request-Id` (`DIV-14`) | ✅ Corroborado por fonte secundaria. `RF-30` nao depende do nome exato, tornando a diferenca inocua |
| Escopos OAuth por endpoint de `files/search_v2`, `sharing/create_shared_link_with_settings`, `users/get_current_account`, `users/get_space_usage` (`DIV-14`) | 🟡 Pendencia residual de baixo risco. Ausencia produz `401` explicito, nao defeito silencioso. Verificar durante a implementacao |
| Definir o titular do copyright (`DP-20`) | 🔴 **BLOQUEIO — exclusivo do solicitante, e agora com custo crescente.** A licenca escolhida (MIT) esta publicada em `LICENSE`, mas o **titular permanece em espaco reservado** (`DIV-E`), e o `LICENSE` **ja foi publicado** — o placeholder esta no historico publico. O Tech Lead perguntou repetidamente, nao houve resposta e **recusa-se a preencher por inferencia**, o que esta correto. A recomendacao de resolver antes do primeiro commit nao pode mais ser cumprida; resolver agora e barato, e encarece a cada commit e a cada contribuidor |
| Plataformas/shells (`DP-07`) e dependencia de `jq` (`DP-08`) | ✅ **RESOLVIDAS — ja estavam, e o registro e que estava desatualizado.** Solicitante respondeu **"so cURL, `bash` 4+, Linux"**. Piso tecnico refinado para **4.4**; RHEL 6 (4.1) fica fora. **`jq` NAO e dependencia** — interpretador JSON proprio. Destrava `lib/preflight` e `lib/json`, e **elimina do plano a camada de compatibilidade GNU/BSD**. Falha de propagacao registrada em `RSK-26` |
| Armazenamento da credencial (`DP-11`) | ✅ **RESOLVIDA.** Arquivo `0600` sob `$XDG_CONFIG_HOME` com recuo para `~/.config`; permissao **verificada no preflight, que recusa a execucao** se estiver ampla; **sem sobrescrita por variavel de ambiente**. Destrava `lib/config`. Contrapartida: container efemero e CI exigem montagem de arquivo, nao injecao de variavel |
| Perfis e multiplas contas (`DP-05`) | ✅ **RESOLVIDA — uma conta so, sem nocao de perfil.** Um unico arquivo de credencial; sem `--profile`, sem arquivo por perfil, sem selecao por ambiente. **Justificativa:** acrescentar perfis depois e **aditivo** e nao quebra o contrato publico congelado por `RF-35`, entao o custo de adiar e baixo e o de antecipar (multiplexacao, mais estados, mais teste) nao se justifica. `RF-05` e `F-18` vao para o backlog. **Combinada com `DP-11`, fecha o desenho de `lib/config`: um arquivo, um caminho, sem multiplexacao** |
| **`RNF-23` — teto de analise e paginacao obrigatoria** | **Novo, derivado de medicao.** `lib/json` tem teto de entrada de **256 KiB** (reduzido de 4 MiB, que extrapolava para ~86 s). Consequencia vinculante: **`lib/http` deve enviar `limit`/`max_results` explicito em toda chamada de colecao** e paginar por cursor. `RF-16` e `RF-22` emendados. **E restricao de projeto entre componentes, nao detalhe interno do analisador** — sem ela, pasta grande falha por recusa de analise (erro interno sem relacao aparente com o tamanho da pasta) em vez de paginar |
| Leitura de estado documental intermediario pelo Senior Developer | ℹ️ **NAO e divergencia.** Ele leu os documentos durante a atualizacao da v0.5 e viu estado transitorio, reportando `DP-07` como aberta e citando `DIV-15`. A v0.5 sanou. Registrado para que nao seja reaberto como divergencia real caso apareca no registro tecnico dele |
| Formato de saida para nomes com quebra de linha (`DIV-16b`) | ✅ **RESOLVIDA.** Linha por padrao; **`--null`** com terminador nulo (`find -print0`/`xargs -0`). **Destrava `lib/output`.** `RNF-22` vale nos dois modos |
| Decidir comandos de paridade no MVP (`DP-06`), politica de log (`DP-10`) e volumes (`DP-12`) | Aberto — nao bloqueiam o inicio; necessarios na Etapa 4 (`DP-06`, `DP-10`) e para calibragem (`DP-12`) |
| Repositorio git e Gitflow (`DP-19`) | ✅ **ENCERRADA.** Solicitante criou e publicou `github.com:salesadriano/dropbox_sync` (`master`); `develop` derivado; trabalho em `feature/*` com Conventional Commits. **Desvios consumados, sem reescrita de historico:** commit inicial sem convencao semantica, e `LICENSE` publicado com titular em espaco reservado |
| Acionar Senior Developer para o ciclo de implementacao com `protocolo-tdd` | ✅ **Etapa 1 concluida.** Camada de dominio entregue e em handoff para o QA |
| Vigiar `RSK-23` durante a implementacao: nao reintroduzir estado local persistente | Ativo. **Preservado na Etapa 1**: nenhuma escrita fora de `mktemp`; ha caso de teste de ausencia de residuo |
| Validacao do QA sobre a camada de dominio | ✅ **APROVADO COM RESSALVA** no parecer final, e **consolidado pelo Tech Lead em 2026-08-18** (`PRJ-DEC-22`). O QA nao persistiu artefatos `.md` proprios nem acionou o `prompt-logger`, por instrucao do Tech Lead; desvio homologado com compensacao em `PRJ-DEC-24` e reconstruido em `docs/registros/2026-08-18_validacao-qa-camada-dominio-registro-retroativo.md`. Detalhamento — residual, nao estrutural, sem escalonamento. Rodada de acabamento aplicada: `R-01` separador interno consumido do texto (quebrava fidelidade e permitia contornar a redacao por byte de controle na chave) · `R-02` aspa escapada encerrando o mascaramento cedo · `R-03` guarda sem teste, mutacao sobrevivente · `R-04` expoente real ~n^1.5 e teto de entrada reduzido de 256 KiB para 16 KiB (pior caso de 4,56 s para 0,10 s) · `R-05` comentario alinhado ao codigo, com requisito de entrada para `lib/output` · `R-06` invariante fixada por teste. Historico — **Ciclo 2 de 3: REPROVADO com 3 altos, sendo 2 REGRESSOES das correcoes do ciclo 1** (`C2-01` evasao de confinamento por raiz com quebra de linha; `C2-02` redacao cubica, 77.683 ms para 4 KB). Solicitante determinou **troca de metodo**, nao correcao de sintoma: redacao virou varredura em passada unica, e `$( )` foi eliminado de todo o componente de caminho. Todos corrigidos e validados por mutacao. Ciclo 1 (historico): REPROVADO com 5 bloqueantes, `D1` corrupcao de caminho com quebra de linha final · `D2` redacao de segredo falhando no formato JSON, o dominante · `D3` conversao quadratica limitando a capacidade real a ~4 GiB · `D4` falha do leitor mascarada, produzindo resumo valido de conteudo truncado · `D5` executor aprovando sem executar e usando `eval` sobre a saida dos testes. Aguardando ciclo 2 |
| `RNF-13` (analise estatica) | ✅ **Atendido.** `shellcheck` 0.10.0 em `~/.local/bin/shellcheck`, exit 0, 7 supressoes com justificativa individual |
| Correcao documental do ciclo 1 | A afirmacao de que a especificacao nao cobria o arquivo vazio era **falsa**: a fonte primaria trata o caso explicitamente. Pendencia fantasma removida |
| Aceite do Tech Lead sobre `PRJ-DEC-10` (codigos 5 a 15) | ✅ **CONCEDIDO em 2026-08-18** (`TL-01`). Encerra `DIV-17`, a divergencia de registro entre RF-35 na v0.4 (faixa "aceita como contrato publico") e a memoria de projeto ("aguarda aceite"): eram **dois gates distintos registrados como se fossem um** — o aceite do solicitante sobre o contrato ja existia, o do Tech Lead era o que faltava. Ambos os registros passam a dizer a mesma coisa |
| **Condicoes de consumo do QA (`R-01` e `R-02`) para `lib/http`** | ✅ **SATISFEITAS**, verificadas de forma independente. Constam do fechamento como condicao cumprida, **nao como pendencia herdada**. `R-01`: o separador interno era consumido do texto do usuario, permitindo contornar a redacao com byte de controle dentro do nome da chave — a pior forma de falha possivel numa funcao de redacao, porque nada no diagnostico denunciava o contorno. `R-02`: aspa escapada encerrava o mascaramento cedo. Ambos corrigidos, com casos de teste dedicados e mutacao que reprova. **`lib/http` esta liberado para alimentar `dbx_errors_redigir` com corpo real da API.** A condicao que permanece para a Etapa 2 e outra e nao se confunde com estas: `RNF-22` |
| Aprovacao **formal** do solicitante sobre os testes do QA (`DEC-STR-07`, item 12 do protocolo) | 🟡 **Pendencia de formalidade.** Nao ha registro com `.claude/agents-protocol/templates/aprovacao-e-reaprovacao-solicitante-template.md`. A aprovacao **substantiva** existe: o solicitante determinou a troca de metodo no ciclo 2, aceitou `C2-08`, rejeitou o codigo 16, aceitou `RSK-24` e a ampliacao de `RNF-20`. Registrar com o template na abertura da Etapa 2, cobrindo retroativamente a suite da Etapa 1 |
| **TOCTOU residual (`D11`)** | ✅ **Aceito como risco residual**, a revisitar quando `DP-07` fechar. Sem mitigacao implementada. Duas correcoes do QA a analise anterior: validar por `readlink` compara **texto de caminho**, mutavel por `rename` entre `open` e leitura — o correto seria **dispositivo+inode**; e `/proc/self/fd` e exclusivo de Linux, entao adota-lo fecharia `DP-07` a forca (mesmo erro de `DIV-B`) |
| Comportamento de `5xx` diante da idempotencia | ✅ **Decidido:** segue a mesma regra de `http=0` — idempotente `recuo_exponencial`, nao idempotente `indeterminado`. O BA ajustara `RNF-07`, escrito antes de a dimensao de idempotencia existir |
| Classe `dependencia_ausente` (codigo 16) | ❌ **Rejeitada.** O problema e precisao de mensagem, nao escassez de codigos, e a tabela publicada e contrato permanente. Executado no lugar: mensagem de `configuracao` reescrita sem presumir credencial, e falha de area temporaria remapeada para `configuracao` (`C2-07`) |
| `DIV-A` System Design x `lib/json` | ✅ **Corrigida na v0.4 do System Design.** Eram **tres** inconsistencias, nao uma: `lib/errors` deixou de declarar dependencia de `lib/json`; `lib/transfer` saiu do dominio para a nova camada de **Orquestracao** (o mais grave — a violacao nasceria com F-01/F-02 na Etapa 2); `lib/hash` foi acrescentado ao dominio, de onde estava ausente. Nenhuma exigiu mudanca de codigo; o diagrama ja refletia o desenho correto |
| `DIV-B` `RNF-01` x `bash` 3.2 | ✅ **ENCERRADA — a divergencia nao existia.** `DP-07` e `DP-08` **ja estavam decididas** pelo solicitante ("so cURL, `bash` 4+, Linux") e nunca haviam sido propagadas aos requisitos. A premissa do Senior Developer estava **correta**. `RNF-01` **descongelado** com piso `bash` **4.4** — refinamento tecnico da decisao, nao decisao nova. RHEL 6 (4.1) fica fora, registrado. `DIV-15` **retirada** e `RSK-25` **encerrado** por evidencia falsa. A falha real — decisao tomada e nao propagada ao artefato — esta em **`RSK-26`** |
| `DIV-C` `RNF-10` x saida orientada a linha | ✅ **ENCERRADA.** **(a)** Defeito ativo em `lib/path` — valor errado com status 0 — corrigido em `C2-01/D1`. **(b)** Divergencia de requisito **resolvida por `DIV-16b`**: saida orientada a linha por padrao, opcao **`--null`** com terminador nulo, no padrao `find -print0`/`xargs -0`. **`lib/output` destravado.** ⚠️ `RNF-22` vale **nos dois modos**: a redacao opera sobre quebras de linha, nao sobre terminadores de registro, e em `--null` a concatenacao indevida destroi **mais** conteudo |
| `DIV-D` confinamento local | ✅ **`RNF-20` emendado na v0.4** para nomear os **dois espacos de nomes**, com resolucao fisica de symlinks, os oito vetores como criterio de aceite, opt-in da raiz `/` com falha fechada (`PRJ-DEC-17`) e TOCTOU como `RSK-24` |
| `DIV-E` titular do copyright | 🔴 **Aberta, urgencia elevada.** Confirmada por leitura direta do `LICENSE` em 2026-08-18. **AgAravada:** o `LICENSE` **ja foi publicado** com o titular em espaco reservado, e o placeholder esta no historico publico do repositorio. Licenca sem titular identificado tem eficacia juridica duvidosa, e formalizar encarece a cada commit e a cada contribuidor. **Unica pendencia do projeto com custo crescente** |
| `RSK-24` TOCTOU | ✅ **ENCERRADO — aceite RECONFIRMADO com `DP-07` ja resolvida.** Registro explicito para que o aceite nao volte a parecer apoiado no argumento que caiu: **o argumento de portabilidade nao integra mais a fundamentacao**, pois a plataforma e Linux por decisao. O solicitante reconfirmou com base apenas nos dois argumentos tecnicos verificados: **(a)** `readlink` compara **texto de caminho**, mutavel por `rename` — a mitigacao correta exigiria **dispositivo+inode**, e nao a que estava avaliada; **(b)** a mitigacao **nao cobre percurso recursivo**, que e o caso real em lote. Ou seja, protegeria o caso menos frequente e deixaria descoberto o mais frequente. Revisitar apenas se surgir mitigacao que cubra recursao e compare identidade de inode |
| `RSK-26` falha de propagacao de decisao | **Novo e ativo. Impacto Alto, probabilidade Alta — ja materializado.** Decisao tomada pelo solicitante e nao refletida no artefato, com o documento desatualizado tratado como fonte de verdade. Custo real: gerou `DIV-15` (falsa acusacao contra a implementacao), congelamento indevido de `RNF-01`, `RSK-25` (risco sobre exemplo inexistente) e distorcao de `RSK-24`. **Regra:** propagar decisao ao artefato **na mesma rodada**; ausencia em documento **nao** e evidencia de ausencia de decisao; divergencia contra a implementacao exige verificar a origem da decisao, nao so o estado do registro |
| `DIV-17` registro do aceite dos codigos 5 a 15 | ✅ **Reconciliada em 2026-08-18** (`TL-02`). RF-35 na v0.4 registrava a faixa como aceita e a memoria marcava o aceite como pendente; eram dois gates distintos, ambos verdadeiros quando escritos. Ver `PRJ-DEC-10` |
| Requisito de entrada de `lib/output` (`R-05`) | ✅ **Formalizado como `RNF-22`.** A redacao de cabecalho sensivel consome o restante da linha, logo `lib/output` **nao pode** concatenar diagnostico na mesma linha de cabecalho sensivel, sob pena de destruir o identificador de correlacao que `RF-30` existe para preservar |
| Medicoes de desempenho da Etapa 1 | ✅ **Incorporadas ao System Design e a `DP-12`.** `content_hash` ~320 MB/s com memoria plana em ~8 MiB **deixa de ser gargalo**; redacao de erro (~n^1.5) entra como novo ponto de atencao. Falta apenas o **volume de negocio** para dimensionar |
| Etapa 2 (`lib/http`, `lib/auth`, `lib/tmp`, `lib/output`) | Nao iniciada, **tecnicamente liberada** pelo aceite da Etapa 1. Continua dependente de `DP-07`, `DP-08`, `DP-11` e `DP-05`. Condicoes de entrada acumuladas: decisao de desenho sobre `DIV-16b` **antes** de `lib/output`; `RNF-22` respeitado; `lib/tmp` herda o contrato de area temporaria 0700/0600 de `PRJ-DEC-13`; gate `PRJ-DEC-23` em vigor; artefato proprio de QA e log de prompt obrigatorios em cada ciclo (`PRJ-DEC-24`); vigiar `RSK-23` em `lib/config` e `lib/tmp` |

## Historico de referencia

- Artefatos da demanda: `docs/requisitos/`, `docs/arquitetura/system-design.md`, `docs/prompts/`.
- Fechamento da Etapa 1: `docs/registros/2026-08-18_revisao-consolidada-etapa-1-camada-dominio.md`, `docs/registros/2026-08-18_aprovacao-final-etapa-1-camada-dominio.md` e `docs/registros/2026-08-18_validacao-qa-camada-dominio-registro-retroativo.md`.
- Registro estrutural do fechamento: [historico/2026-08-18-0001-fechamento-etapa-1-camada-dominio.md](historico/2026-08-18-0001-fechamento-etapa-1-camada-dominio.md).
- Mudancas estruturais relevantes desta memoria devem gerar registro em `historico/`.
